import type {
  MenuCategory,
  MenuItem,
  RestaurantCatalog,
} from "@/types/restaurant";

export type MenuSection = MenuCategory;
export type MenuEntry = MenuItem;
export type MenuCatalog = RestaurantCatalog;
